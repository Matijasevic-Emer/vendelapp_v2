<div id="<%= dom_id category %>">
  <p>
    <%= category.name %>
    <%= link_to t('.edit'), edit_category_path(category) %>
    <%= link_to t('.delete'), category_path(category), data: { turbo_confirm: t('common.confirm'), turbo_method: :delete }, class: "delete-link" %>

  </p>

</div>
